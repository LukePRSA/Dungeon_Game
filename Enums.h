#ifndef ENUMS_H
#define ENUMS_H
// useful enums used in room_object and its children

// options for shape of Room_object (aside from rectangle, which is addressed )
enum Shape
{
square,
circle
};

// rotation of an object
enum Rotation
{
right,
down,
left,
up
};

#endif